<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Rofikul Islam — Flutter & Android Developer</title>
  <link
    href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@300;400;500;600;700&family=Syne:wght@400;600;700;800&family=JetBrains+Mono:wght@400;500&display=swap"
    rel="stylesheet">
  <style>
    :root {
      --flutter-blue: #0553B1;
      --flutter-sky: #54C5F8;
      --flutter-teal: #01579B;
      --android-green: #3DDC84;
      --hot-pink: #FF2D78;
      --amber: #FFB300;
      --purple: #7C4DFF;
      --bg-dark: #050A14;
      --bg-card: #0D1525;
      --text-primary: #F0F4FF;
      --text-muted: #7A8BAA;
      --glow-blue: rgba(84, 197, 248, 0.4);
      --glow-green: rgba(61, 220, 132, 0.4);
    }

    *,
    *::before,
    *::after {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    html {
      scroll-behavior: smooth;
    }

    body {
      background: var(--bg-dark);
      color: var(--text-primary);
      font-family: 'Space Grotesk', sans-serif;
      overflow-x: hidden;
      cursor: none;
    }

    /* Custom Cursor */
    .cursor {
      width: 12px;
      height: 12px;
      background: var(--flutter-sky);
      border-radius: 50%;
      position: fixed;
      pointer-events: none;
      z-index: 9999;
      transform: translate(-50%, -50%);
      transition: transform 0.1s, width 0.2s, height 0.2s, background 0.2s;
      mix-blend-mode: screen;
    }

    .cursor-ring {
      width: 36px;
      height: 36px;
      border: 2px solid rgba(84, 197, 248, 0.5);
      border-radius: 50%;
      position: fixed;
      pointer-events: none;
      z-index: 9998;
      transform: translate(-50%, -50%);
      transition: transform 0.15s ease-out, width 0.3s, height 0.3s;
    }

    body:hover .cursor {
      opacity: 1;
    }

    /* Noise overlay */
    body::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)' opacity='0.03'/%3E%3C/svg%3E");
      pointer-events: none;
      z-index: 1;
      opacity: 0.4;
    }

    /* ===== NAV ===== */
    nav {
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      z-index: 100;
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 20px 60px;
      background: rgba(5, 10, 20, 0.8);
      backdrop-filter: blur(20px);
      border-bottom: 1px solid rgba(84, 197, 248, 0.1);
      transition: all 0.3s;
    }

    nav.scrolled {
      padding: 14px 60px;
    }

    .nav-logo {
      font-family: 'Syne', sans-serif;
      font-weight: 800;
      font-size: 1.4rem;
      background: linear-gradient(135deg, var(--flutter-sky), var(--android-green));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      letter-spacing: -0.5px;
    }

    .nav-links {
      display: flex;
      gap: 36px;
      list-style: none;
    }

    .nav-links a {
      color: var(--text-muted);
      text-decoration: none;
      font-size: 0.9rem;
      font-weight: 500;
      letter-spacing: 0.5px;
      transition: color 0.3s;
      position: relative;
    }

    .nav-links a::after {
      content: '';
      position: absolute;
      bottom: -4px;
      left: 0;
      width: 0;
      height: 2px;
      background: var(--flutter-sky);
      transition: width 0.3s;
    }

    .nav-links a:hover {
      color: var(--flutter-sky);
    }

    .nav-links a:hover::after {
      width: 100%;
    }

    .nav-cta {
      padding: 10px 24px;
      border: 1.5px solid var(--flutter-sky);
      color: var(--flutter-sky);
      border-radius: 8px;
      text-decoration: none;
      font-size: 0.85rem;
      font-weight: 600;
      transition: all 0.3s;
    }

    .nav-cta:hover {
      background: var(--flutter-sky);
      color: var(--bg-dark);
    }

    /* ===== HERO ===== */
    #hero {
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      position: relative;
      overflow: hidden;
      padding: 120px 60px 80px;
    }

    .hero-bg-grid {
      position: absolute;
      inset: 0;
      background-image:
        linear-gradient(rgba(84, 197, 248, 0.04) 1px, transparent 1px),
        linear-gradient(90deg, rgba(84, 197, 248, 0.04) 1px, transparent 1px);
      background-size: 50px 50px;
      animation: gridPulse 8s ease-in-out infinite;
    }

    @keyframes gridPulse {

      0%,
      100% {
        opacity: 0.5;
      }

      50% {
        opacity: 1;
      }
    }

    .orb {
      position: absolute;
      border-radius: 50%;
      filter: blur(80px);
      pointer-events: none;
    }

    .orb-1 {
      width: 500px;
      height: 500px;
      background: radial-gradient(circle, rgba(84, 197, 248, 0.15), transparent 70%);
      top: -100px;
      right: -100px;
      animation: float1 10s ease-in-out infinite;
    }

    .orb-2 {
      width: 400px;
      height: 400px;
      background: radial-gradient(circle, rgba(61, 220, 132, 0.12), transparent 70%);
      bottom: -50px;
      left: -100px;
      animation: float2 12s ease-in-out infinite;
    }

    .orb-3 {
      width: 300px;
      height: 300px;
      background: radial-gradient(circle, rgba(124, 77, 255, 0.1), transparent 70%);
      top: 50%;
      left: 40%;
      animation: float3 8s ease-in-out infinite;
    }

    @keyframes float1 {

      0%,
      100% {
        transform: translate(0, 0)
      }

      50% {
        transform: translate(-30px, 40px)
      }
    }

    @keyframes float2 {

      0%,
      100% {
        transform: translate(0, 0)
      }

      50% {
        transform: translate(40px, -30px)
      }
    }

    @keyframes float3 {

      0%,
      100% {
        transform: translate(0, 0) scale(1)
      }

      50% {
        transform: translate(-20px, 20px) scale(1.1)
      }
    }

    .hero-content {
      position: relative;
      z-index: 2;
      max-width: 900px;
      text-align: center;
    }

    .hero-badge {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      padding: 8px 18px;
      border: 1px solid rgba(61, 220, 132, 0.4);
      border-radius: 100px;
      background: rgba(61, 220, 132, 0.07);
      font-size: 0.8rem;
      color: var(--android-green);
      font-family: 'JetBrains Mono', monospace;
      margin-bottom: 32px;
      animation: fadeUp 0.8s ease both;
    }

    .badge-dot {
      width: 7px;
      height: 7px;
      border-radius: 50%;
      background: var(--android-green);
      animation: pulse 2s infinite;
    }

    @keyframes pulse {

      0%,
      100% {
        box-shadow: 0 0 0 0 rgba(61, 220, 132, 0.7)
      }

      50% {
        box-shadow: 0 0 0 8px rgba(61, 220, 132, 0)
      }
    }

    .hero-name {
      font-family: 'Syne', sans-serif;
      font-size: clamp(3rem, 7vw, 6rem);
      font-weight: 800;
      line-height: 1;
      letter-spacing: -2px;
      margin-bottom: 16px;
      animation: fadeUp 0.8s 0.2s ease both;
    }

    .hero-name .line1 {
      display: block;
      background: linear-gradient(135deg, #fff 0%, var(--flutter-sky) 50%, var(--android-green) 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-size: 200% 200%;
      animation: fadeUp 0.8s 0.2s ease both, gradientShift 4s ease-in-out infinite;
    }

    @keyframes gradientShift {

      0%,
      100% {
        background-position: 0% 50%
      }

      50% {
        background-position: 100% 50%
      }
    }

    .hero-role {
      font-size: clamp(1rem, 2.5vw, 1.4rem);
      color: var(--text-muted);
      margin-bottom: 24px;
      animation: fadeUp 0.8s 0.4s ease both;
    }

    .role-tag {
      display: inline-block;
      background: linear-gradient(135deg, var(--flutter-blue), var(--purple));
      color: white;
      padding: 4px 14px;
      border-radius: 6px;
      font-size: 0.85rem;
      font-weight: 600;
      margin: 0 4px;
    }

    .hero-desc {
      font-size: 1.05rem;
      color: var(--text-muted);
      line-height: 1.7;
      max-width: 600px;
      margin: 0 auto 48px;
      animation: fadeUp 0.8s 0.6s ease both;
    }

    .hero-actions {
      display: flex;
      gap: 16px;
      justify-content: center;
      flex-wrap: wrap;
      animation: fadeUp 0.8s 0.8s ease both;
    }

    .btn-primary {
      display: inline-flex;
      align-items: center;
      gap: 10px;
      padding: 16px 36px;
      background: linear-gradient(135deg, var(--flutter-sky), var(--android-green));
      color: var(--bg-dark);
      text-decoration: none;
      border-radius: 12px;
      font-weight: 700;
      font-size: 0.95rem;
      transition: all 0.3s;
      border: none;
      cursor: none;
    }

    .btn-primary:hover {
      transform: translateY(-3px);
      box-shadow: 0 20px 50px rgba(84, 197, 248, 0.3);
    }

    .btn-secondary {
      display: inline-flex;
      align-items: center;
      gap: 10px;
      padding: 16px 36px;
      border: 2px solid rgba(84, 197, 248, 0.3);
      color: var(--flutter-sky);
      text-decoration: none;
      border-radius: 12px;
      font-weight: 600;
      font-size: 0.95rem;
      transition: all 0.3s;
      background: rgba(84, 197, 248, 0.05);
      cursor: none;
    }

    .btn-secondary:hover {
      border-color: var(--flutter-sky);
      background: rgba(84, 197, 248, 0.1);
      transform: translateY(-3px);
    }

    .hero-stats {
      display: flex;
      gap: 48px;
      justify-content: center;
      margin-top: 70px;
      animation: fadeUp 0.8s 1s ease both;
    }

    .stat {
      text-align: center;
    }

    .stat-num {
      font-family: 'Syne', sans-serif;
      font-size: 2.2rem;
      font-weight: 800;
      background: linear-gradient(135deg, var(--flutter-sky), var(--android-green));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      display: block;
    }

    .stat-label {
      font-size: 0.8rem;
      color: var(--text-muted);
      letter-spacing: 1px;
      text-transform: uppercase;
    }

    @keyframes fadeUp {
      from {
        opacity: 0;
        transform: translateY(30px);
      }

      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    /* ===== TECH TICKER ===== */
    .ticker-wrapper {
      position: relative;
      overflow: hidden;
      padding: 20px 0;
      border-top: 1px solid rgba(255, 255, 255, 0.05);
      border-bottom: 1px solid rgba(255, 255, 255, 0.05);
      background: rgba(84, 197, 248, 0.03);
    }

    .ticker-wrapper::before,
    .ticker-wrapper::after {
      content: '';
      position: absolute;
      top: 0;
      bottom: 0;
      width: 100px;
      z-index: 2;
    }

    .ticker-wrapper::before {
      left: 0;
      background: linear-gradient(90deg, var(--bg-dark), transparent);
    }

    .ticker-wrapper::after {
      right: 0;
      background: linear-gradient(-90deg, var(--bg-dark), transparent);
    }

    .ticker-track {
      display: flex;
      gap: 50px;
      animation: ticker 25s linear infinite;
      width: max-content;
    }

    @keyframes ticker {
      from {
        transform: translateX(0);
      }

      to {
        transform: translateX(-50%);
      }
    }

    .ticker-item {
      display: flex;
      align-items: center;
      gap: 10px;
      color: var(--text-muted);
      font-size: 0.85rem;
      font-weight: 500;
      white-space: nowrap;
    }

    .ticker-item span.icon {
      font-size: 1.1rem;
    }

    /* ===== SECTIONS ===== */
    section {
      padding: 100px 60px;
      max-width: 1200px;
      margin: 0 auto;
    }

    .section-tag {
      font-family: 'JetBrains Mono', monospace;
      font-size: 0.75rem;
      color: var(--flutter-sky);
      letter-spacing: 3px;
      text-transform: uppercase;
      display: flex;
      align-items: center;
      gap: 12px;
      margin-bottom: 16px;
    }

    .section-tag::before {
      content: '';
      width: 30px;
      height: 1px;
      background: var(--flutter-sky);
    }

    .section-title {
      font-family: 'Syne', sans-serif;
      font-size: clamp(2rem, 4vw, 3rem);
      font-weight: 800;
      line-height: 1.1;
      letter-spacing: -1px;
      margin-bottom: 60px;
    }

    .section-title em {
      font-style: normal;
      background: linear-gradient(135deg, var(--flutter-sky), var(--android-green));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }

    /* ===== SKILLS ===== */
    #skills {
      position: relative;
    }

    .skills-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 24px;
    }

    .skill-card {
      background: var(--bg-card);
      border: 1px solid rgba(255, 255, 255, 0.07);
      border-radius: 20px;
      padding: 32px;
      position: relative;
      overflow: hidden;
      transition: all 0.4s;
      cursor: none;
    }

    .skill-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: var(--card-glow);
      opacity: 0;
      transition: opacity 0.4s;
      border-radius: 20px;
    }

    .skill-card:hover {
      transform: translateY(-8px);
      border-color: rgba(84, 197, 248, 0.3);
    }

    .skill-card:hover::before {
      opacity: 1;
    }

    .skill-card.flutter {
      --card-glow: linear-gradient(135deg, rgba(84, 197, 248, 0.08), transparent);
    }

    .skill-card.android {
      --card-glow: linear-gradient(135deg, rgba(61, 220, 132, 0.08), transparent);
    }

    .skill-card.backend {
      --card-glow: linear-gradient(135deg, rgba(124, 77, 255, 0.08), transparent);
    }

    .skill-card.tools {
      --card-glow: linear-gradient(135deg, rgba(255, 179, 0, 0.08), transparent);
    }

    .skill-icon {
      width: 56px;
      height: 56px;
      border-radius: 14px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.6rem;
      margin-bottom: 20px;
    }

    .skill-card.flutter .skill-icon {
      background: rgba(84, 197, 248, 0.15);
    }

    .skill-card.android .skill-icon {
      background: rgba(61, 220, 132, 0.15);
    }

    .skill-card.backend .skill-icon {
      background: rgba(124, 77, 255, 0.15);
    }

    .skill-card.tools .skill-icon {
      background: rgba(255, 179, 0, 0.15);
    }

    .skill-title {
      font-family: 'Syne', sans-serif;
      font-size: 1.2rem;
      font-weight: 700;
      margin-bottom: 8px;
    }

    .skill-desc {
      font-size: 0.88rem;
      color: var(--text-muted);
      line-height: 1.6;
      margin-bottom: 20px;
    }

    .skill-tags {
      display: flex;
      flex-wrap: wrap;
      gap: 8px;
    }

    .skill-tag {
      font-family: 'JetBrains Mono', monospace;
      font-size: 0.72rem;
      padding: 4px 12px;
      border-radius: 100px;
      border: 1px solid;
    }

    .flutter .skill-tag {
      color: var(--flutter-sky);
      border-color: rgba(84, 197, 248, 0.3);
      background: rgba(84, 197, 248, 0.08);
    }

    .android .skill-tag {
      color: var(--android-green);
      border-color: rgba(61, 220, 132, 0.3);
      background: rgba(61, 220, 132, 0.08);
    }

    .backend .skill-tag {
      color: #BCA0FF;
      border-color: rgba(124, 77, 255, 0.3);
      background: rgba(124, 77, 255, 0.08);
    }

    .tools .skill-tag {
      color: var(--amber);
      border-color: rgba(255, 179, 0, 0.3);
      background: rgba(255, 179, 0, 0.08);
    }

    /* ===== PROJECTS ===== */
    #projects {
      background: rgba(13, 21, 37, 0.5);
      border-radius: 40px;
      margin: 0 20px;
      max-width: none;
    }

    .projects-wrapper {
      max-width: 1200px;
      margin: 0 auto;
    }

    .projects-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(340px, 1fr));
      gap: 28px;
    }

    .project-card {
      background: var(--bg-dark);
      border: 1px solid rgba(255, 255, 255, 0.08);
      border-radius: 24px;
      overflow: hidden;
      transition: all 0.4s;
      cursor: none;
      position: relative;
    }

    .project-card:hover {
      transform: translateY(-10px);
      box-shadow: 0 30px 80px rgba(0, 0, 0, 0.5);
    }

    .project-thumb {
      height: 200px;
      display: flex;
      align-items: center;
      justify-content: center;
      position: relative;
      overflow: hidden;
    }

    .project-thumb-1 {
      background: linear-gradient(135deg, #0553B1 0%, #54C5F8 100%);
    }

    .project-thumb-2 {
      background: linear-gradient(135deg, #1B5E20 0%, #3DDC84 100%);
    }

    .project-thumb-3 {
      background: linear-gradient(135deg, #4A148C 0%, #7C4DFF 100%);
    }

    .project-thumb-4 {
      background: linear-gradient(135deg, #B71C1C 0%, #FF2D78 100%);
    }

    .project-thumb-5 {
      background: linear-gradient(135deg, #E65100 0%, #FFB300 100%);
    }

    .project-thumb-6 {
      background: linear-gradient(135deg, #006064 0%, #00BCD4 100%);
    }

    .project-mock {
      font-size: 4rem;
      filter: drop-shadow(0 10px 30px rgba(0, 0, 0, 0.4));
      animation: floatPhone 4s ease-in-out infinite;
    }

    @keyframes floatPhone {

      0%,
      100% {
        transform: translateY(0) rotate(-5deg)
      }

      50% {
        transform: translateY(-15px) rotate(5deg)
      }
    }

    .project-badge {
      position: absolute;
      top: 16px;
      right: 16px;
      padding: 5px 12px;
      border-radius: 100px;
      font-size: 0.72rem;
      font-weight: 600;
      font-family: 'JetBrains Mono', monospace;
    }

    .badge-flutter {
      background: rgba(84, 197, 248, 0.2);
      color: var(--flutter-sky);
      border: 1px solid rgba(84, 197, 248, 0.4);
    }

    .badge-android {
      background: rgba(61, 220, 132, 0.2);
      color: var(--android-green);
      border: 1px solid rgba(61, 220, 132, 0.4);
    }

    .project-body {
      padding: 28px;
    }

    .project-title {
      font-family: 'Syne', sans-serif;
      font-size: 1.25rem;
      font-weight: 700;
      margin-bottom: 10px;
    }

    .project-desc {
      font-size: 0.87rem;
      color: var(--text-muted);
      line-height: 1.6;
      margin-bottom: 20px;
    }

    .project-tech {
      display: flex;
      flex-wrap: wrap;
      gap: 8px;
      margin-bottom: 24px;
    }

    .project-tech span {
      font-family: 'JetBrains Mono', monospace;
      font-size: 0.7rem;
      padding: 3px 10px;
      background: rgba(255, 255, 255, 0.05);
      border: 1px solid rgba(255, 255, 255, 0.1);
      border-radius: 100px;
      color: var(--text-muted);
    }

    .project-links {
      display: flex;
      gap: 12px;
    }

    .project-link {
      flex: 1;
      text-align: center;
      padding: 10px;
      border-radius: 10px;
      font-size: 0.82rem;
      font-weight: 600;
      text-decoration: none;
      transition: all 0.3s;
      cursor: none;
    }

    .link-github {
      background: rgba(255, 255, 255, 0.07);
      color: var(--text-primary);
      border: 1px solid rgba(255, 255, 255, 0.1);
    }

    .link-github:hover {
      background: rgba(255, 255, 255, 0.15);
    }

    .link-demo {
      background: linear-gradient(135deg, var(--flutter-sky), var(--android-green));
      color: var(--bg-dark);
    }

    .link-demo:hover {
      transform: translateY(-2px);
      box-shadow: 0 8px 24px rgba(84, 197, 248, 0.3);
    }

    /* ===== EXPERIENCE ===== */
    .timeline {
      position: relative;
      padding-left: 30px;
    }

    .timeline::before {
      content: '';
      position: absolute;
      left: 0;
      top: 0;
      bottom: 0;
      width: 2px;
      background: linear-gradient(180deg, var(--flutter-sky), var(--android-green), var(--purple));
      border-radius: 2px;
    }

    .timeline-item {
      position: relative;
      padding: 0 0 56px 40px;
      opacity: 0;
      transform: translateX(-20px);
      transition: all 0.6s ease;
    }

    .timeline-item.visible {
      opacity: 1;
      transform: translateX(0);
    }

    .timeline-dot {
      position: absolute;
      left: -8px;
      top: 6px;
      width: 18px;
      height: 18px;
      border-radius: 50%;
      background: var(--bg-dark);
      border: 3px solid var(--flutter-sky);
      box-shadow: 0 0 20px var(--glow-blue);
    }

    .timeline-period {
      font-family: 'JetBrains Mono', monospace;
      font-size: 0.75rem;
      color: var(--flutter-sky);
      margin-bottom: 8px;
      letter-spacing: 1px;
    }

    .timeline-title {
      font-family: 'Syne', sans-serif;
      font-size: 1.3rem;
      font-weight: 700;
      margin-bottom: 4px;
    }

    .timeline-company {
      font-size: 0.9rem;
      color: var(--android-green);
      font-weight: 600;
      margin-bottom: 12px;
    }

    .timeline-desc {
      font-size: 0.9rem;
      color: var(--text-muted);
      line-height: 1.7;
    }

    /* ===== SKILLS BARS ===== */
    .skills-bars {
      max-width: 700px;
    }

    .skill-bar-item {
      margin-bottom: 28px;
    }

    .skill-bar-header {
      display: flex;
      justify-content: space-between;
      margin-bottom: 10px;
    }

    .skill-bar-name {
      font-weight: 600;
      font-size: 0.95rem;
    }

    .skill-bar-pct {
      font-family: 'JetBrains Mono', monospace;
      font-size: 0.82rem;
      color: var(--text-muted);
    }

    .skill-bar-track {
      height: 8px;
      background: rgba(255, 255, 255, 0.06);
      border-radius: 100px;
      overflow: hidden;
    }

    .skill-bar-fill {
      height: 100%;
      border-radius: 100px;
      width: 0;
      transition: width 1.5s cubic-bezier(0.4, 0, 0.2, 1);
    }

    .fill-flutter {
      background: linear-gradient(90deg, var(--flutter-blue), var(--flutter-sky));
    }

    .fill-android {
      background: linear-gradient(90deg, #1B5E20, var(--android-green));
    }

    .fill-dart {
      background: linear-gradient(90deg, var(--flutter-teal), #64B5F6);
    }

    .fill-firebase {
      background: linear-gradient(90deg, #E65100, var(--amber));
    }

    .fill-kotlin {
      background: linear-gradient(90deg, #4A148C, var(--purple));
    }

    /* ===== CONTACT ===== */
    #contact {
      text-align: center;
    }

    .contact-card {
      background: var(--bg-card);
      border: 1px solid rgba(84, 197, 248, 0.2);
      border-radius: 32px;
      padding: 80px 60px;
      position: relative;
      overflow: hidden;
      max-width: 800px;
      margin: 0 auto;
    }

    .contact-card::before {
      content: '';
      position: absolute;
      top: -50%;
      left: -50%;
      width: 200%;
      height: 200%;
      background: conic-gradient(from 0deg at 50% 50%,
          rgba(84, 197, 248, 0) 0deg,
          rgba(84, 197, 248, 0.07) 60deg,
          rgba(61, 220, 132, 0.07) 120deg,
          rgba(124, 77, 255, 0.07) 180deg,
          rgba(84, 197, 248, 0) 360deg);
      animation: spin 20s linear infinite;
    }

    @keyframes spin {
      to {
        transform: rotate(360deg);
      }
    }

    .contact-card>* {
      position: relative;
      z-index: 2;
    }

    .contact-email {
      font-family: 'Syne', sans-serif;
      font-size: clamp(1.4rem, 3vw, 2.2rem);
      font-weight: 700;
      color: var(--flutter-sky);
      text-decoration: none;
      display: block;
      margin: 24px 0;
      transition: all 0.3s;
      cursor: none;
    }

    .contact-email:hover {
      color: var(--android-green);
    }

    .social-links {
      display: flex;
      gap: 16px;
      justify-content: center;
      margin-top: 36px;
    }

    .social-link {
      width: 52px;
      height: 52px;
      border-radius: 14px;
      background: rgba(255, 255, 255, 0.05);
      border: 1px solid rgba(255, 255, 255, 0.1);
      display: flex;
      align-items: center;
      justify-content: center;
      text-decoration: none;
      font-size: 1.2rem;
      transition: all 0.3s;
      cursor: none;
    }

    .social-link:hover {
      background: rgba(84, 197, 248, 0.15);
      border-color: rgba(84, 197, 248, 0.4);
      transform: translateY(-4px);
    }

    /* ===== EDUCATION ===== */
    #education {
      position: relative;
    }

    .edu-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
      gap: 24px;
    }

    .edu-card {
      background: var(--bg-card);
      border: 1px solid rgba(255, 255, 255, 0.07);
      border-radius: 24px;
      padding: 36px 32px;
      position: relative;
      overflow: hidden;
      transition: all 0.4s;
      cursor: none;
    }

    .edu-card::before {
      content: '';
      position: absolute;
      inset: 0;
      opacity: 0;
      transition: opacity 0.4s;
      border-radius: 24px;
      background: var(--edu-glow, linear-gradient(135deg, rgba(84, 197, 248, 0.08), transparent));
    }

    .edu-card:hover {
      transform: translateY(-8px);
      border-color: rgba(84, 197, 248, 0.3);
    }

    .edu-card:hover::before {
      opacity: 1;
    }

    .edu-icon {
      width: 56px;
      height: 56px;
      border-radius: 16px;
      background: rgba(84, 197, 248, 0.12);
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.6rem;
      margin-bottom: 20px;
    }

    .edu-card.purple .edu-icon {
      background: rgba(124, 77, 255, 0.12);
    }

    .edu-card.amber .edu-icon {
      background: rgba(255, 179, 0, 0.12);
    }

    .edu-degree {
      font-family: 'Syne', sans-serif;
      font-size: 1.15rem;
      font-weight: 700;
      margin-bottom: 6px;
    }

    .edu-school {
      font-size: 0.9rem;
      color: var(--flutter-sky);
      font-weight: 600;
      margin-bottom: 4px;
    }

    .edu-card.purple .edu-school {
      color: #BCA0FF;
    }

    .edu-card.amber .edu-school {
      color: var(--amber);
    }

    .edu-period {
      font-family: 'JetBrains Mono', monospace;
      font-size: 0.72rem;
      color: var(--text-muted);
      letter-spacing: 1px;
      margin-bottom: 16px;
    }

    .edu-desc {
      font-size: 0.87rem;
      color: var(--text-muted);
      line-height: 1.65;
      margin-bottom: 20px;
    }

    .edu-tags {
      display: flex;
      flex-wrap: wrap;
      gap: 8px;
    }

    .edu-tag {
      font-family: 'JetBrains Mono', monospace;
      font-size: 0.7rem;
      padding: 4px 12px;
      border-radius: 100px;
      border: 1px solid rgba(84, 197, 248, 0.3);
      color: var(--flutter-sky);
      background: rgba(84, 197, 248, 0.08);
    }

    .edu-card.purple .edu-tag {
      color: #BCA0FF;
      border-color: rgba(124, 77, 255, 0.3);
      background: rgba(124, 77, 255, 0.08);
    }

    .edu-card.amber .edu-tag {
      color: var(--amber);
      border-color: rgba(255, 179, 0, 0.3);
      background: rgba(255, 179, 0, 0.08);
    }

    /* ===== FOOTER ===== */
    footer {
      text-align: center;
      padding: 40px 60px;
      border-top: 1px solid rgba(255, 255, 255, 0.05);
      color: var(--text-muted);
      font-size: 0.85rem;
    }

    footer span {
      background: linear-gradient(135deg, var(--flutter-sky), var(--android-green));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      font-weight: 600;
    }

    /* ===== SCROLL REVEAL ===== */
    .reveal {
      opacity: 0;
      transform: translateY(40px);
      transition: all 0.7s cubic-bezier(0.4, 0, 0.2, 1);
    }

    .reveal.visible {
      opacity: 1;
      transform: translateY(0);
    }

    /* ===== RESPONSIVE ===== */
    @media (max-width: 768px) {
      nav {
        padding: 16px 24px;
      }

      .nav-links {
        display: none;
      }

      section {
        padding: 80px 24px;
      }

      #hero {
        padding: 120px 24px 60px;
      }

      .hero-stats {
        gap: 24px;
      }

      .projects-grid {
        grid-template-columns: 1fr;
      }

      .contact-card {
        padding: 48px 24px;
      }

      footer {
        padding: 30px 24px;
      }
    }

    /* ===== FLOATING PARTICLES ===== */
    .particles {
      position: fixed;
      inset: 0;
      pointer-events: none;
      z-index: 0;
    }

    .particle {
      position: absolute;
      width: 3px;
      height: 3px;
      border-radius: 50%;
      background: var(--flutter-sky);
      opacity: 0;
      animation: particleFloat var(--dur, 8s) var(--delay, 0s) ease-in-out infinite;
    }

    @keyframes particleFloat {
      0% {
        transform: translate(0, 100vh);
        opacity: 0;
      }

      10% {
        opacity: 0.6;
      }

      90% {
        opacity: 0.6;
      }

      100% {
        transform: translate(var(--tx, 0), -100px);
        opacity: 0;
      }
    }
  </style>
</head>

<body>

  <!-- Custom cursor -->
  <div class="cursor" id="cursor"></div>
  <div class="cursor-ring" id="cursorRing"></div>

  <!-- Particles -->
  <div class="particles" id="particles"></div>

  <!-- Nav -->
  <nav id="navbar">
    <div class="nav-logo">RI.dev</div>
    <ul class="nav-links">
      <li><a href="#skills">Skills</a></li>
      <li><a href="#projects">Projects</a></li>
      <li><a href="#experience">Experience</a></li>
      <li><a href="#education">Education</a></li>
      <li><a href="#contact">Contact</a></li>
    </ul>
    <a href="#contact" class="nav-cta">Hire Me</a>
  </nav>

  <!-- Hero -->
  <section id="hero">
    <div class="hero-bg-grid"></div>
    <div class="orb orb-1"></div>
    <div class="orb orb-2"></div>
    <div class="orb orb-3"></div>

    <div class="hero-content">
      <div class="hero-badge">
        <div class="badge-dot"></div>
        Available for freelance & full-time roles
      </div>

      <h1 class="hero-name">
        <span class="line1">Rana Sheikh</span>
      </h1>

      <p class="hero-role">
        <span class="role-tag">Flutter</span> &amp;
        <span class="role-tag" style="background:linear-gradient(135deg,#1B5E20,#3DDC84)">Android</span>
        Developer
      </p>

      <p class="hero-desc">
        Crafting beautiful cross-platform mobile apps with Flutter &amp; native Android experiences that users love.
        From pixel-perfect UI to production-ready architecture.
      </p>

      <div class="hero-actions">
        <a href="#projects" class="btn-primary">
          🚀 View My Work
        </a>
        <a href="#contact" class="btn-secondary">
          ✉️ Get In Touch
        </a>
      </div>

      <div class="hero-stats">
        <div class="stat">
          <span class="stat-num" data-target="20">0</span>
          <span class="stat-label">Apps Built</span>
        </div>
        <div class="stat">
          <span class="stat-num" data-target="3">0</span>
          <span class="stat-label">Years Exp</span>
        </div>
        <div class="stat">
          <span class="stat-num" data-target="15">0</span>
          <span class="stat-label">Happy Clients</span>
        </div>
        <div class="stat">
          <span class="stat-num" data-target="50">0</span>
          <span class="stat-label">GitHub Repos</span>
        </div>
      </div>
    </div>
  </section>

  <!-- Tech Ticker -->
  <div class="ticker-wrapper">
    <div class="ticker-track" id="ticker">
      <div class="ticker-item"><span class="icon">🐦</span> Flutter</div>
      <div class="ticker-item"><span class="icon">🤖</span> Android</div>
      <div class="ticker-item"><span class="icon">🎯</span> Dart</div>
      <div class="ticker-item"><span class="icon">🔥</span> Firebase</div>
      <div class="ticker-item"><span class="icon">🟣</span> Kotlin</div>
      <div class="ticker-item"><span class="icon">📦</span> GetX / BLoC</div>
      <div class="ticker-item"><span class="icon">🔌</span> REST API</div>
      <div class="ticker-item"><span class="icon">☁️</span> Google Cloud</div>
      <div class="ticker-item"><span class="icon">🗄️</span> SQLite</div>
      <div class="ticker-item"><span class="icon">🎨</span> Material 3</div>
      <div class="ticker-item"><span class="icon">⚡</span> Riverpod</div>
      <div class="ticker-item"><span class="icon">🧪</span> Unit Testing</div>
      <div class="ticker-item"><span class="icon">🐦</span> Flutter</div>
      <div class="ticker-item"><span class="icon">🤖</span> Android</div>
      <div class="ticker-item"><span class="icon">🎯</span> Dart</div>
      <div class="ticker-item"><span class="icon">🔥</span> Firebase</div>
      <div class="ticker-item"><span class="icon">🟣</span> Kotlin</div>
      <div class="ticker-item"><span class="icon">📦</span> GetX / BLoC</div>
      <div class="ticker-item"><span class="icon">🔌</span> REST API</div>
      <div class="ticker-item"><span class="icon">☁️</span> Google Cloud</div>
      <div class="ticker-item"><span class="icon">🗄️</span> SQLite</div>
      <div class="ticker-item"><span class="icon">🎨</span> Material 3</div>
      <div class="ticker-item"><span class="icon">⚡</span> Riverpod</div>
      <div class="ticker-item"><span class="icon">🧪</span> Unit Testing</div>
    </div>
  </div>

  <!-- Skills -->
  <section id="skills">
    <div class="section-tag">What I Do</div>
    <h2 class="section-title reveal">My <em>Technical</em> Arsenal</h2>
    <div class="skills-grid">
      <div class="skill-card flutter reveal">
        <div class="skill-icon">🐦</div>
        <div class="skill-title">Flutter Development</div>
        <div class="skill-desc">Building beautiful cross-platform apps for iOS & Android with Flutter, pixel-perfect UI
          and buttery 60fps animations.</div>
        <div class="skill-tags">
          <span class="skill-tag">Dart</span>
          <span class="skill-tag">Flutter</span>
          <span class="skill-tag">BLoC</span>
          <span class="skill-tag">GetX</span>
          <span class="skill-tag">Riverpod</span>
          <span class="skill-tag">Provider</span>
        </div>
      </div>
      <div class="skill-card android reveal">
        <div class="skill-icon">🤖</div>
        <div class="skill-title">Native Android</div>
        <div class="skill-desc">Deep knowledge of native Android development with Kotlin & Java, Jetpack Compose, MVVM
          architecture and Jetpack libraries.</div>
        <div class="skill-tags">
          <span class="skill-tag">Kotlin</span>
          <span class="skill-tag">Jetpack</span>
          <span class="skill-tag">Compose</span>
          <span class="skill-tag">Room DB</span>
          <span class="skill-tag">ViewModel</span>
        </div>
      </div>
      <div class="skill-card backend reveal">
        <div class="skill-icon">🔥</div>
        <div class="skill-title">Backend Integration</div>
        <div class="skill-desc">Seamless API integration with REST & GraphQL, real-time data with Firebase, push
          notifications and cloud functions.</div>
        <div class="skill-tags">
          <span class="skill-tag">Firebase</span>
          <span class="skill-tag">REST API</span>
          <span class="skill-tag">GraphQL</span>
          <span class="skill-tag">FCM</span>
          <span class="skill-tag">Dio</span>
        </div>
      </div>
      <div class="skill-card tools reveal">
        <div class="skill-icon">🛠️</div>
        <div class="skill-title">Dev Tools & Workflow</div>
        <div class="skill-desc">Proficient in modern dev workflows including CI/CD pipelines, version control, testing
          strategies and Play Store deployments.</div>
        <div class="skill-tags">
          <span class="skill-tag">Git</span>
          <span class="skill-tag">GitHub Actions</span>
          <span class="skill-tag">Fastlane</span>
          <span class="skill-tag">Figma</span>
          <span class="skill-tag">Postman</span>
        </div>
      </div>
    </div>

    <!-- Skill Bars -->
    <div style="margin-top:60px">
      <h3 style="font-family:'Syne',sans-serif;font-size:1.5rem;font-weight:700;margin-bottom:36px" class="reveal">
        Proficiency Levels</h3>
      <div class="skills-bars reveal">
        <div class="skill-bar-item">
          <div class="skill-bar-header">
            <span class="skill-bar-name">🐦 Flutter</span>
            <span class="skill-bar-pct">92%</span>
          </div>
          <div class="skill-bar-track">
            <div class="skill-bar-fill fill-flutter" data-width="92"></div>
          </div>
        </div>
        <div class="skill-bar-item">
          <div class="skill-bar-header">
            <span class="skill-bar-name">🤖 Native Android (Kotlin)</span>
            <span class="skill-bar-pct">85%</span>
          </div>
          <div class="skill-bar-track">
            <div class="skill-bar-fill fill-android" data-width="85"></div>
          </div>
        </div>
        <div class="skill-bar-item">
          <div class="skill-bar-header">
            <span class="skill-bar-name">🎯 Dart</span>
            <span class="skill-bar-pct">90%</span>
          </div>
          <div class="skill-bar-track">
            <div class="skill-bar-fill fill-dart" data-width="90"></div>
          </div>
        </div>
        <div class="skill-bar-item">
          <div class="skill-bar-header">
            <span class="skill-bar-name">🔥 Firebase</span>
            <span class="skill-bar-pct">88%</span>
          </div>
          <div class="skill-bar-track">
            <div class="skill-bar-fill fill-firebase" data-width="88"></div>
          </div>
        </div>
        <div class="skill-bar-item">
          <div class="skill-bar-header">
            <span class="skill-bar-name">🟣 Kotlin</span>
            <span class="skill-bar-pct">83%</span>
          </div>
          <div class="skill-bar-track">
            <div class="skill-bar-fill fill-kotlin" data-width="83"></div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Projects -->
  <section id="projects" style="padding:100px 40px">
    <div class="projects-wrapper">
      <div class="section-tag">Portfolio</div>
      <h2 class="section-title reveal">Featured <em>Projects</em></h2>
      <div class="projects-grid">
        <div class="project-card reveal">
          <div class="project-thumb project-thumb-1">
            <div class="project-mock">📱</div>
            <div class="project-badge badge-flutter">Flutter</div>
          </div>
          <div class="project-body">
            <div class="project-title">ShopEase — E-Commerce App</div>
            <div class="project-desc">Full-featured shopping app with Flutter, BLoC state management, Stripe payments,
              and real-time inventory updates.</div>
            <div class="project-tech">
              <span>Flutter</span><span>BLoC</span><span>Stripe</span><span>Firebase</span><span>Dart</span>
            </div>
            <div class="project-links">
              <a href="#" class="project-link link-github">⭐ GitHub</a>
              <a href="#" class="project-link link-demo">▶ Live Demo</a>
            </div>
          </div>
        </div>

        <div class="project-card reveal">
          <div class="project-thumb project-thumb-2">
            <div class="project-mock">🏃</div>
            <div class="project-badge badge-android">Android</div>
          </div>
          <div class="project-body">
            <div class="project-title">FitTrack — Health & Fitness</div>
            <div class="project-desc">Native Android fitness tracker with Jetpack Compose UI, Room database, Google Fit
              API integration and live workout analysis.</div>
            <div class="project-tech">
              <span>Kotlin</span><span>Compose</span><span>Room DB</span><span>Google Fit</span>
            </div>
            <div class="project-links">
              <a href="#" class="project-link link-github">⭐ GitHub</a>
              <a href="#" class="project-link link-demo">▶ Play Store</a>
            </div>
          </div>
        </div>

        <div class="project-card reveal">
          <div class="project-thumb project-thumb-3">
            <div class="project-mock">💬</div>
            <div class="project-badge badge-flutter">Flutter</div>
          </div>
          <div class="project-body">
            <div class="project-title">ChatNow — Messaging App</div>
            <div class="project-desc">Real-time chat app with end-to-end encryption, Firebase Realtime DB, push
              notifications and story feature.</div>
            <div class="project-tech">
              <span>Flutter</span><span>Firebase</span><span>FCM</span><span>GetX</span>
            </div>
            <div class="project-links">
              <a href="#" class="project-link link-github">⭐ GitHub</a>
              <a href="#" class="project-link link-demo">▶ Demo</a>
            </div>
          </div>
        </div>

        <div class="project-card reveal">
          <div class="project-thumb project-thumb-4">
            <div class="project-mock">📰</div>
            <div class="project-badge badge-flutter">Flutter</div>
          </div>
          <div class="project-body">
            <div class="project-title">NewsWave — News Aggregator</div>
            <div class="project-desc">Personalized news app with NewsAPI integration, offline reading, bookmarks and
              push notification for breaking news.</div>
            <div class="project-tech">
              <span>Flutter</span><span>Riverpod</span><span>REST API</span><span>Hive</span>
            </div>
            <div class="project-links">
              <a href="#" class="project-link link-github">⭐ GitHub</a>
              <a href="#" class="project-link link-demo">▶ Demo</a>
            </div>
          </div>
        </div>

        <div class="project-card reveal">
          <div class="project-thumb project-thumb-5">
            <div class="project-mock">🍔</div>
            <div class="project-badge badge-flutter">Flutter</div>
          </div>
          <div class="project-body">
            <div class="project-title">FoodZone — Food Delivery</div>
            <div class="project-desc">Complete food delivery platform with real-time order tracking, maps integration,
              restaurant discovery and payment gateway.</div>
            <div class="project-tech">
              <span>Flutter</span><span>Google Maps</span><span>GetX</span><span>Razorpay</span>
            </div>
            <div class="project-links">
              <a href="#" class="project-link link-github">⭐ GitHub</a>
              <a href="#" class="project-link link-demo">▶ Demo</a>
            </div>
          </div>
        </div>

        <div class="project-card reveal">
          <div class="project-thumb project-thumb-6">
            <div class="project-mock">📚</div>
            <div class="project-badge badge-android">Android</div>
          </div>
          <div class="project-body">
            <div class="project-title">LearnX — EdTech Platform</div>
            <div class="project-desc">Online learning app with video courses, quizzes, progress tracking, certificates
              and offline content download.</div>
            <div class="project-tech">
              <span>Kotlin</span><span>ExoPlayer</span><span>MVVM</span><span>Retrofit</span>
            </div>
            <div class="project-links">
              <a href="#" class="project-link link-github">⭐ GitHub</a>
              <a href="#" class="project-link link-demo">▶ Demo</a>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Experience -->
  <section id="experience">
    <div class="section-tag">Journey</div>
    <h2 class="section-title reveal">My <em>Experience</em></h2>
    <div class="timeline">
      <div class="timeline-item">
        <div class="timeline-dot"></div>
        <div class="timeline-period">2023 — Present</div>
        <div class="timeline-title">Senior Flutter Developer</div>
        <div class="timeline-company">Freelance / Remote</div>
        <div class="timeline-desc">Building production-grade Flutter apps for clients worldwide. Specialized in
          e-commerce, healthcare and fintech domains. Delivered 10+ apps on Play Store & App Store.</div>
      </div>
      <div class="timeline-item">
        <div class="timeline-dot" style="border-color:var(--android-green);box-shadow:0 0 20px var(--glow-green)"></div>
        <div class="timeline-period">2022 — 2023</div>
        <div class="timeline-title">Android Developer</div>
        <div class="timeline-company">TechStartup BD, Dhaka</div>
        <div class="timeline-desc">Native Android development using Kotlin and Jetpack libraries. Built and maintained
          5+ apps. Migrated legacy Java codebase to Kotlin with MVVM architecture.</div>
      </div>
      <div class="timeline-item">
        <div class="timeline-dot" style="border-color:var(--purple);box-shadow:0 0 20px rgba(124,77,255,0.4)"></div>
        <div class="timeline-period">2021 — 2022</div>
        <div class="timeline-title">Junior Flutter Developer</div>
        <div class="timeline-company">DevAgency, Dhaka</div>
        <div class="timeline-desc">Started Flutter journey building cross-platform apps. Learned BLoC pattern, Firebase
          integration and REST API consumption. Shipped 3 apps to production.</div>
      </div>
      <div class="timeline-item">
        <div class="timeline-dot" style="border-color:var(--amber);box-shadow:0 0 20px rgba(255,179,0,0.4)"></div>
        <div class="timeline-period">2020 — 2021</div>
        <div class="timeline-title">CS Graduate</div>
        <div class="timeline-company">University, Bangladesh</div>
        <div class="timeline-desc">Bachelor's in Computer Science. Final year project: AI-powered health monitoring
          Android app. Won Best Project Award at department showcase.</div>
      </div>
    </div>
  </section>

  <!-- Education -->
  <section id="education">
    <div class="section-tag">Education</div>
    <h2 class="section-title reveal">My <em>Academic</em> Background</h2>
    <div class="timeline">

      <!-- Entry 1 -->
      <div class="timeline-item">
        <div class="timeline-dot"></div>
        <div class="timeline-period">2017 — 2021</div>
        <div class="timeline-title">B.Sc. in Computer Science &amp; Engineering</div>
        <div class="timeline-company">University of Dhaka, Bangladesh &nbsp;🎓</div>
        <div class="timeline-desc">Graduated with distinction. Core coursework in Data Structures, Algorithms, Operating
          Systems, Software Engineering and Mobile App Development. Final year project: AI-powered health monitoring
          Android app — won Best Project Award at the department showcase.</div>
        <div class="skill-tags" style="margin-top:14px;display:flex;flex-wrap:wrap;gap:8px">
          <span class="skill-tag"
            style="color:var(--flutter-sky);border-color:rgba(84,197,248,0.3);background:rgba(84,197,248,0.08)">CGPA 3.8
            / 4.0</span>
          <span class="skill-tag"
            style="color:var(--flutter-sky);border-color:rgba(84,197,248,0.3);background:rgba(84,197,248,0.08)">Best
            Project Award</span>
          <span class="skill-tag"
            style="color:var(--flutter-sky);border-color:rgba(84,197,248,0.3);background:rgba(84,197,248,0.08)">Dean's
            List</span>
        </div>
      </div>

      <!-- Entry 2 -->
      <div class="timeline-item">
        <div class="timeline-dot" style="border-color:var(--android-green);box-shadow:0 0 20px var(--glow-green)"></div>
        <div class="timeline-period">2022</div>
        <div class="timeline-title">Android &amp; Flutter Development</div>
        <div class="timeline-company">Google Developer Certification &nbsp;📱</div>
        <div class="timeline-desc">Completed Google's official Android Developer certification covering Jetpack Compose,
          Kotlin Coroutines, Material Design 3, and modern MVVM architecture patterns for production apps.</div>
        <div class="skill-tags" style="margin-top:14px;display:flex;flex-wrap:wrap;gap:8px">
          <span class="skill-tag"
            style="color:var(--android-green);border-color:rgba(61,220,132,0.3);background:rgba(61,220,132,0.08)">Certified</span>
          <span class="skill-tag"
            style="color:var(--android-green);border-color:rgba(61,220,132,0.3);background:rgba(61,220,132,0.08)">Kotlin</span>
          <span class="skill-tag"
            style="color:var(--android-green);border-color:rgba(61,220,132,0.3);background:rgba(61,220,132,0.08)">Jetpack
            Compose</span>
        </div>
      </div>

      <!-- Entry 3 -->
      <div class="timeline-item">
        <div class="timeline-dot" style="border-color:var(--purple);box-shadow:0 0 20px rgba(124,77,255,0.4)"></div>
        <div class="timeline-period">2023</div>
        <div class="timeline-title">Firebase &amp; Cloud Architecture</div>
        <div class="timeline-company">Coursera / Google Cloud &nbsp;🔥</div>
        <div class="timeline-desc">Completed advanced coursework in Firebase ecosystem — Cloud Functions, Firestore data
          modeling, Remote Config and Analytics for scalable production mobile applications.</div>
        <div class="skill-tags" style="margin-top:14px;display:flex;flex-wrap:wrap;gap:8px">
          <span class="skill-tag"
            style="color:#BCA0FF;border-color:rgba(124,77,255,0.3);background:rgba(124,77,255,0.08)">Firebase</span>
          <span class="skill-tag"
            style="color:#BCA0FF;border-color:rgba(124,77,255,0.3);background:rgba(124,77,255,0.08)">Cloud
            Functions</span>
          <span class="skill-tag"
            style="color:#BCA0FF;border-color:rgba(124,77,255,0.3);background:rgba(124,77,255,0.08)">Firestore</span>
        </div>
      </div>

    </div>
  </section>

  <!-- Contact -->
  <section id="contact">
    <div class="section-tag">Let's Work</div>
    <h2 class="section-title reveal">Get In <em>Touch</em></h2>
    <div class="contact-card reveal">
      <div style="font-size:3rem;margin-bottom:16px">👋</div>
      <h3 style="font-family:'Syne',sans-serif;font-size:1.8rem;font-weight:800;margin-bottom:12px">Have a project in
        mind?</h3>
      <p style="color:var(--text-muted);max-width:500px;margin:0 auto;line-height:1.7">
        I'm available for Flutter & Android freelance projects and full-time roles. Let's build something amazing
        together!
      </p>
      <a href="mailto:rofikul6424islam@gmail.com" class="contact-email">rofikul6424islam@gmail.com</a>
      <div>
        <a href="#" class="btn-primary" style="display:inline-flex">✉️ Send Message</a>
      </div>
      <div class="social-links">
        <a href="#" class="social-link" title="GitHub">🐙</a>
        <a href="#" class="social-link" title="LinkedIn">💼</a>
        <a href="#" class="social-link" title="Twitter">🐦</a>
        <a href="#" class="social-link" title="Play Store">🎮</a>
        <a href="#" class="social-link" title="WhatsApp">📱</a>
      </div>
    </div>
  </section>

  <!-- Footer -->
  <footer>
    <p>Made with <span>Flutter love ❤️</span> by Rana Sheikh• 2025</p>
    <p style="margin-top:8px;font-size:0.78rem">Built with HTML, CSS & JS — Inspired by Flutter's design language</p>
  </footer>

  <script>
    // Custom cursor
    const cursor = document.getElementById('cursor');
    const ring = document.getElementById('cursorRing');
    let mx = 0, my = 0, rx = 0, ry = 0;
    document.addEventListener('mousemove', e => {
      mx = e.clientX; my = e.clientY;
      cursor.style.left = mx + 'px';
      cursor.style.top = my + 'px';
    });
    function lerp(a, b, t) { return a + (b - a) * t }
    function animCursor() {
      rx = lerp(rx, mx, 0.12);
      ry = lerp(ry, my, 0.12);
      ring.style.left = rx + 'px';
      ring.style.top = ry + 'px';
      requestAnimationFrame(animCursor);
    }
    animCursor();
    document.querySelectorAll('a, button').forEach(el => {
      el.addEventListener('mouseenter', () => {
        cursor.style.width = '20px'; cursor.style.height = '20px';
        ring.style.width = '50px'; ring.style.height = '50px';
      });
      el.addEventListener('mouseleave', () => {
        cursor.style.width = '12px'; cursor.style.height = '12px';
        ring.style.width = '36px'; ring.style.height = '36px';
      });
    });

    // Navbar scroll
    const navbar = document.getElementById('navbar');
    window.addEventListener('scroll', () => {
      navbar.classList.toggle('scrolled', window.scrollY > 50);
    });

    // Particles
    const pContainer = document.getElementById('particles');
    for (let i = 0; i < 30; i++) {
      const p = document.createElement('div');
      p.className = 'particle';
      const colors = ['var(--flutter-sky)', 'var(--android-green)', 'var(--purple)', 'var(--amber)'];
      p.style.cssText = `
      left: ${Math.random() * 100}%;
      background: ${colors[Math.floor(Math.random() * colors.length)]};
      --dur: ${6 + Math.random() * 10}s;
      --delay: ${Math.random() * 8}s;
      --tx: ${(Math.random() - 0.5) * 200}px;
      width: ${2 + Math.random() * 3}px;
      height: ${2 + Math.random() * 3}px;
    `;
      pContainer.appendChild(p);
    }

    // Scroll reveal
    const observer = new IntersectionObserver((entries) => {
      entries.forEach((entry, i) => {
        if (entry.isIntersecting) {
          setTimeout(() => entry.target.classList.add('visible'), i * 80);
        }
      });
    }, { threshold: 0.1 });

    document.querySelectorAll('.reveal').forEach(el => observer.observe(el));
    document.querySelectorAll('.timeline-item').forEach(el => observer.observe(el));
    // Also observe education timeline items (same selector covers both sections)

    // Counter animation
    function animateCounter(el) {
      const target = parseInt(el.dataset.target);
      const suffix = el.textContent.includes('+') ? '+' : '';
      let start = 0;
      const step = target / 60;
      const timer = setInterval(() => {
        start = Math.min(start + step, target);
        el.textContent = Math.floor(start) + (start >= target ? '+' : '');
        if (start >= target) clearInterval(timer);
      }, 25);
    }
    const counterObs = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          animateCounter(entry.target);
          counterObs.unobserve(entry.target);
        }
      });
    }, { threshold: 0.5 });
    document.querySelectorAll('[data-target]').forEach(el => counterObs.observe(el));

    // Skill bars
    const barObs = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.querySelectorAll('.skill-bar-fill').forEach(bar => {
            bar.style.width = bar.dataset.width + '%';
          });
          barObs.unobserve(entry.target);
        }
      });
    }, { threshold: 0.3 });
    document.querySelectorAll('.skills-bars').forEach(el => barObs.observe(el));

    // Active nav link
    const sections = document.querySelectorAll('section[id]');
    window.addEventListener('scroll', () => {
      let current = '';
      sections.forEach(s => {
        if (window.scrollY >= s.offsetTop - 200) current = s.id;
      });
      document.querySelectorAll('.nav-links a').forEach(a => {
        a.style.color = a.getAttribute('href') === '#' + current ? 'var(--flutter-sky)' : '';
      });
    });
  </script>
</body>

</html>